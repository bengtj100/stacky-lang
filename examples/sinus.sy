#!/bin/env -S stacky -b
```
[ "................................................................................"
  swap left ] 'spaces;

[
    1 swap 0.2
    [
        "|" put
        dup  sin 30.0 * 40.0 + floor
        over cos 30.0 * 40.0 + floor
        2 ndup min dup spaces "*" ++ put
        lrot max dup lrot swap - spaces "*" ++ put
        80 swap - spaces "|" ++ putLn
    ]
    for
] 'sinus;

2000 sinus
```
